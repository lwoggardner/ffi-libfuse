# frozen_string_literal: true

source 'https://rubygems.org'

gem 'ffi', path: '../ffi' if Dir.exist?(ENV.fetch('FFI_LOCAL', '../ffi'))

# Specify your gem's dependencies in ffi-libfuse.gemspec
gemspec
